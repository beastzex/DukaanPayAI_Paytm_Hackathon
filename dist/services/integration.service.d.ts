import { WebhookSubscriptionEntity } from '../domain/entities/entities';
import { AIProposedAction, MerchantAIContext } from '../integrations/ai-connector/ai-service-connector';
export declare class IntegrationService {
    private webhookRepo;
    /**
     * Provides full merchant context to external AI models and agents.
     */
    getMerchantAIContext(merchantId: string): Promise<MerchantAIContext>;
    /**
     * Accepts actions and growth recommendations generated by the external AI team.
     */
    ingestAIAction(action: AIProposedAction): Promise<{
        actionId: string;
        status: string;
        queuedForApproval: boolean;
    }>;
    /**
     * Registers a new generic webhook endpoint for external consumer teams.
     */
    registerWebhook(merchantId: string, targetUrl: string, subscribedEvents: string[]): Promise<WebhookSubscriptionEntity>;
    /**
     * Triggers an outbound webhook dispatch with HMAC signing.
     */
    triggerWebhook(subscriptionId: string, event: string, data: any): Promise<boolean>;
}
//# sourceMappingURL=integration.service.d.ts.map